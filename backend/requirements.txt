# LyricSpot backend — full install (text search + voice input).
# NOTE: ffmpeg must be installed separately (system dependency for audio conversion)
#   Ubuntu/Debian: sudo apt install ffmpeg · macOS: brew install ffmpeg
# Install torch CPU-only first for a smaller footprint:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
-r requirements-core.txt
openai-whisper==20231117
librosa==0.10.1
soundfile==0.12.1
numpy>=1.21.0,<2.0.0
